spring:
  application:
    name: guowang-waveform-analysis
  web:
    resources:
      static-locations: classpath:/static/

  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB